# Foundations status

Integration branch: `may/foundations-sep-25-integration`, based on `751354f` and merged with `origin/main` at `35ef3fd`. The branch composes work from #83, #99, #84, and #90. No pull request is being created or updated for this mission.

| Workstream | Implemented in branch | Wired end-to-end | Tested | Benchmarked | Integrated |
| --- | --- | --- | --- | --- | --- |
| Shared contracts | Separate document, view, workspace, file, owner, result, and stream identities | Yes | Typecheck and focused contract tests | No dedicated latency result | Yes |
| A: documents and views | Live target and save guards, inactive mounted rich/source edits, lifecycle and targeted save APIs, public `editorViews`, and two editable split panes | Yes for supported document and view targets | Model, service, split-pane, and installed-addon desktop tests; final full gate pending | Paired general input; targeted edit and full two-pane latency pending | Yes |
| B: commands and shortcuts | Command-owned in-app and global shortcuts, atomic replacement, Addons menu, palette/context/toolbar target capture, and quick-note command composition | Yes for current command and menu slots | Focused unit and desktop tests | Command dispatch and disabled-addon cost pending | Yes |
| C: workspace | Sequenced snapshot/changes, paged listing, scoped text/binary IO, exclusive create, and versioned closed-file update/move/trash | Yes for current file and event slices | Focused service and desktop bridge tests | Per-operation IPC and scale runs pending | Yes |
| D: addon storage | Global/workspace/session stores, lifecycle, and export-option migration | Yes for current scopes | Focused service, migration, and desktop tests | Retained owner memory pending | Yes |
| E: metadata and queries | Incremental references/tags/properties/headings, flavor-aware interpretation, bounded cursor search, and shared Graph/Tags/Backlinks consumers | Yes for supported syntax | Focused service, flavor, and desktop consumer tests | Candidate-only 100/2,000-note index runs; final-head and end-to-end cost pending | Yes |
| F: editor providers | Bounded completion, hover, and action brokers; source/rich adapters; writing suggestions | Yes for supported mappings | Focused broker and installed-addon desktop tests | Completion latency pending | Yes |
| G: privileged integrations | One-use selected text, prompted HTTPS GET text, credential vault, and selected import/export handles | Yes for current narrow grants | Focused service and installed-addon desktop tests | Activation and operation cost pending | Yes |
| H: proof consumer and SDK | Independently installed public-API addon, deterministic document model, and type-only SDK tarball | Yes for supported slices | Proof desktop consumer, packed SDK, and standalone type consumer on macOS; hosted Windows tar check pending | Declaration compile measured on earlier source; packed and final-head cost pending | Yes |

The latest completed local full check, at `1e05ad1`, passed 907 tests, failed one Graph empty-state desktop assertion under full-suite load, and skipped two. That assertion passed in a focused rerun. A previous full check at `600e4a7` passed 879 tests with two skips, before split panes and later metadata changes. Focused integration tests passed 28/28 at `86571c8`; the production build, copy check, and documentation check passed after the note-syntax import split at `625b2c5`. These results do not replace an exact-head full check.

The hosted matrix at `86571c8` exposed a Windows archive-path error in the packed SDK test. A test-only fix is on the integration branch, but a new exact-head Windows result is pending. Paired startup and input evidence compares baseline `35ef3fd` with earlier candidate `fb0a56a`; the final source and wider performance gates remain unmeasured. See `BENCHMARKS.md` for measured conditions and limits and `OPEN_ISSUES.md` for remaining correctness and platform work.
