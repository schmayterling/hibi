<!-- Generated by scripts/docs.mjs. -->

# DialogApi

Type alias · [Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L36)

Open dialogs that share the app's focus handling and keyboard controls.

<details>
<summary>Declaration</summary>

```typescript
type DialogApi = {
  /** Open custom content and receive a handle for closing it or awaiting its result. */
  open: <T = void>(options: DialogOptions<T>) => DialogHandle<T>
  /** Show a message and wait for dismissal. */
  alert: (options: MessageDialogOptions) => Promise<void>
  /** Ask for confirmation. Cancellation returns false. */
  confirm: (
    options: MessageDialogOptions & {
      cancelLabel?: string
      /** Emphasize a destructive action and require an explicit button press. */
      destructive?: boolean
    },
  ) => Promise<boolean>
  /** Ask for a text value. Cancellation returns null. */
  prompt: (options: PromptDialogOptions) => Promise<string | null>
  /** Report whether a shared dialog is currently open. */
  isOpen: () => boolean
}
```

</details>

## Contents

**Methods**

- [open](#open)
- [alert](#alert)
- [confirm](#confirm)
- [prompt](#prompt)
- [isOpen](#isopen)

## Methods

### open

[Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L38)

Open custom content and receive a handle for closing it or awaiting its result.

```typescript
open: <T = void>(options: DialogOptions<T>) => DialogHandle<T>
```

Related: [DialogOptions](DialogOptions.md), [DialogHandle](DialogHandle.md).

**Parameters**

| Name | Type |
| --- | --- |
| <code>options</code> | <code><a href="DialogOptions.md">DialogOptions</a>&lt;T&gt;</code> |

**Returns:** <code><a href="DialogHandle.md">DialogHandle</a>&lt;T&gt;</code>

### alert

[Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L40)

Show a message and wait for dismissal.

```typescript
alert: (options: MessageDialogOptions) => Promise<void>
```

Related: [MessageDialogOptions](MessageDialogOptions.md).

**Parameters**

| Name | Type |
| --- | --- |
| <code>options</code> | <code><a href="MessageDialogOptions.md">MessageDialogOptions</a></code> |

**Returns:** <code>Promise&lt;void&gt;</code>

### confirm

[Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L42)

Ask for confirmation. Cancellation returns false.

```typescript
confirm: (
    options: MessageDialogOptions & {
      cancelLabel?: string
      /** Emphasize a destructive action and require an explicit button press. */
      destructive?: boolean
    },
  ) => Promise<boolean>
```

Related: [MessageDialogOptions](MessageDialogOptions.md).

**Parameters**

| Name | Type |
| --- | --- |
| <code>options</code> | <code><a href="MessageDialogOptions.md">MessageDialogOptions</a> &amp; { cancelLabel?: string destructive?: boolean }</code> |

**Returns:** <code>Promise&lt;boolean&gt;</code>

### prompt

[Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L50)

Ask for a text value. Cancellation returns null.

```typescript
prompt: (options: PromptDialogOptions) => Promise<string | null>
```

Related: [PromptDialogOptions](PromptDialogOptions.md).

**Parameters**

| Name | Type |
| --- | --- |
| <code>options</code> | <code><a href="PromptDialogOptions.md">PromptDialogOptions</a></code> |

**Returns:** <code>Promise&lt;string &#124; null&gt;</code>

### isOpen

[Source](https://github.com/schmayterling/hibi/blob/main/src/ui/dialogs.ts#L52)

Report whether a shared dialog is currently open.

```typescript
isOpen: () => boolean
```

**Returns:** <code>boolean</code>

## Related types

[DialogOptions](DialogOptions.md), [DialogHandle](DialogHandle.md), [MessageDialogOptions](MessageDialogOptions.md), [PromptDialogOptions](PromptDialogOptions.md).
