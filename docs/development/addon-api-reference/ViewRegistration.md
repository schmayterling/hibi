<!-- Generated by scripts/docs.mjs. -->

# ViewRegistration

Type alias · [Source](https://github.com/schmayterling/hibi/blob/main/src/addons/api.ts#L524)

<details>
<summary>Declaration</summary>

```typescript
type ViewRegistration = {
  /** Reuse a local instance ID, or omit it for the default instance. At most eight instances per addon. */
  open: (options?: {
    id?: string
    input?: unknown
    binding?: 'follow' | 'pinned'
    focus?: boolean
    /** Override the sidebar's preferred side. Ignored for panels and tabs. */
    side?: 'left' | 'right'
  }) => ViewInstance
  dispose: () => void
}
```

</details>

## Contents

**Methods**

- [open](#open)
- [dispose](#dispose)

## Methods

### open

[Source](https://github.com/schmayterling/hibi/blob/main/src/addons/api.ts#L526)

Reuse a local instance ID, or omit it for the default instance. At most eight instances per addon.

```typescript
open: (options?: {
    id?: string
    input?: unknown
    binding?: 'follow' | 'pinned'
    focus?: boolean
    /** Override the sidebar's preferred side. Ignored for panels and tabs. */
    side?: 'left' | 'right'
  }) => ViewInstance
```

Related: [ViewInstance](ViewInstance.md).

**Parameters**

| Name | Type |
| --- | --- |
| <code>options?</code> | <code>{ id?: string input?: unknown binding?: 'follow' &#124; 'pinned' focus?: boolean side?: 'left' &#124; 'right' }</code> |

**Returns:** <code><a href="ViewInstance.md">ViewInstance</a></code>

### dispose

[Source](https://github.com/schmayterling/hibi/blob/main/src/addons/api.ts#L534)

```typescript
dispose: () => void
```

**Returns:** <code>void</code>

## Related types

[ViewInstance](ViewInstance.md).
